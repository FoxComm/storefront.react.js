
export type Country = {
  id: number,
  name: string,
  alpha2: string,
  alpha3: string,
  code: string,
  continent: string,
  currency: string,
  uses_postal_code: boolean,
  is_billable: boolean,
  is_shippable: boolean,
}